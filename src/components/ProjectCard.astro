---
import type { Project } from '../data/projects';

const { project } = Astro.props;
---
<a
  href={project.githubUrl}
  target="_blank"
  rel="noopener noreferrer"
  class="tilt-card group block overflow-hidden rounded-xl border border-violet-500/20 bg-gradient-to-br from-violet-950/10 to-slate-900/50 transition hover:border-violet-400/50 hover:shadow-lg hover:shadow-violet-500/10 focus:outline-none focus:ring-2 focus:ring-violet-400 focus:ring-offset-2 focus:ring-offset-slate-950"
>
  <div class="aspect-video overflow-hidden bg-slate-800">
    <img
      src={project.image}
      alt={project.title}
      class="h-full w-full object-cover transition group-hover:scale-105"
      loading="lazy"
    />
  </div>
  <div class="p-5">
    <span class="text-xs font-medium uppercase tracking-wider text-violet-400">{project.category}</span>
    <h3 class="mt-1 text-lg font-semibold text-slate-100 group-hover:text-violet-400 transition">{project.title}</h3>
    <p class="mt-1 text-sm text-slate-400">{project.tagline}</p>
    <p class="mt-2 text-sm text-slate-500">{project.description}</p>
    <span class="mt-3 inline-flex items-center gap-1 text-sm font-medium text-violet-400 group-hover:text-violet-300">
      View on GitHub
      <svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14" /></svg>
    </span>
  </div>
</a>
